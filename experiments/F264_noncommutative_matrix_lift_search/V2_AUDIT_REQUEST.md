# F264-D02 fresh hostile pre-run audit request

Authenticate every path in `V2_FROZEN.sha256`. Treat V1 and its hostile FAIL
audit as immutable history. Use static inspection only unless the packet is no
longer blocked by an incompatible experiment.

Audit these decisive claims from first principles:

1. the ternary enumerator cannot retain the zero vector and recovers exactly
   the five frozen nonzero relations;
2. the associator matrix has the six ordered scalar summands in
   `V2_ALGEBRA.md`, without aggregation or commutation;
3. family 10 emits one projected minor for all three pairs among
   `K,C_parent,C_word`;
4. every order trial checks all 14 original and conjugate root proposals and
   output preserves the exact lcm and new-bit convention;
5. primitive cleanup makes no false gcd-invariance claim, and units remain in
   both family accumulators;
6. word, pair, and triple scope selection uses the complete public scope,
   factor-blind bounded rules, and no collision refill;
7. prime, safe-prime, consecutive, cohort, source-unit, and scope-step loops
   all have deterministic exhaustion gates;
8. ten named global zeros are separately recorded and original/conjugate
   `r,C,K,k,H` streams are separately counted and hashed;
9. P205 rows preserve exact residual gcds, integer residual loss, exact
   baseline multipliers, fixed cell summaries, and the preregistered lead
   predicate;
10. the source remains genuinely noncommutative, not only a trace or cyclic
    control in disguise;
11. the runner firewall covers every executable stage, including split
    boundaries, and the memory, time, output, retry, report, and resource gates
    match `V2_PREREGISTRATION.md` exactly;
12. no hidden label selects a source, scope, candidate, order trial, or held-out
    threshold.

Return **PASS** only if no blocker remains. Preserve the audit as a new file.
Do not edit any V1 artifact or durable ledger.
