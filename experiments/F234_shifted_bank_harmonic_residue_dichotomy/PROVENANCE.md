# F234 provenance

The user emphasized Las Vegas progress, integer-derived information, and
lcm accumulation of certified common orders.  The root then asked for the
exact expected residual log mass of an arbitrary public shifted-child bank
and whether one large residual prime can be covered without already
revealing a known factor residue.

P202 supplies the arbitrary-word return formula.  F233 turns deterministic
shifted banks into projective covers of the hidden gap ratio.  F234 observes
that maximal word saturation is a prime-support indicator, so linearity of
expectation gives the exact log law for every joint bank distribution.  The
same child divisibility equation gives the unordered residue row
`{1,N mod ell}`.  Therefore a single terminal-size captured prime belongs
to the deterministic known-residue route; multiple subterminal unoriented
rows, or hidden high primary valuations above a small exposed prime, give
the word route a distinct role.

The final symbolic audit made the operational quantifiers explicit.  The
algorithm cannot recognize which child primes are residual-supported, so it
tries every exposed prime, every observed child valuation level, and both
orientations.  This remains QP in the aggregate child bit length.  Saturating
an exposed prime to exponent `n` is deliberately stronger than the residue
modulus certified by its observed child valuation.

No computation, web search, literature claim, or durable-ledger edit is
part of this candidate.
