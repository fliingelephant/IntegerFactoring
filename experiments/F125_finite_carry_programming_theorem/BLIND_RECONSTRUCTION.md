# F125 proof-blind reconstruction

**Verdict:** pass.

The reconstructor received only the theorem statement and these key ideas:
CRT conditions for \(N\), fixed arithmetic progressions for \(p,q\), and
optional congruences modulo \(r_i^2\). It independently reconstructed:

- a reduced combined CRT class;
- primes in disjoint proportional intervals;
- balance, infinitude, and \(p,q>n^2\);
- the least-positive inverse bound;
- both direct-sign identities;
- the forced block argument from the prime seed endpoint; and
- valuation-one private rows for pairwise-distinct carries.

It independently gave the same limit: fixed finite prescriptions only. It
found no implication for the growing quasipolynomial source or its complete
decoder.
