# F208 provenance

F208 begins with P179's first twisted divisor coefficient

\[
 A(N)=\sum_{d\mid N}d\chi_4(d).
\]

P179 proves that its exact corrected value is an orientation selector and is
polynomial-time equivalent to the promised semiprime factorization. It does
not evaluate the coefficient. P181 tests binary q-product norms, scalar
binary-orbit modular monomials, fixed linear binary Mahler equations, and
fixed odd-prime root norms. F208 is materially different: it audits direct
integer transforms of the coefficient.

The closest earlier floor result is P166. It proves the totalized floor
reciprocity identity with its exact gcd correction for a segment-Jacobi
zero interface. F208 does not claim that identity as new. It specializes and
combines it with the balanced half-root interval and the twisted character.
This yields the exact one-spike aggregate

\[
 2\sum_{B/2<d\leq B}\chi_4(d)\Delta_N(d)
 =\chi_4(p)(p-1).
\]

The new packet also records the mutually consistent locations of the same
target in:

1. the adjacent reciprocal-floor divisor shell;
2. a triangular HNF determinant shell;
3. noncoprime Dedekind reduction and cotangent poles;
4. the nontrivial diagonal reduced form of discriminant \(-4N\);
5. pair-symmetric divisor representation totals; and
6. the ordinary difference-of-squares magnitude.

The identities are proved from first principles except for the standard
two-square formula

\[
 r_2(n)=4\sum_{d\mid n}\chi_4(d),
\]

which is used only as a comparison for the representation-count boundary.

No web search or literature search was used. No mathematical computation,
computer algebra, or numerical experiment was run. Hashing is used only to
freeze the packet. No durable registry, proved ledger, failed ledger, or
progress ledger is edited by F208.

No priority claim is made. The result is a narrow named-model boundary, not
a lower bound against nonlinear compressed integer selectors and not a
top-level factoring theorem.
