# F206 provenance

F206 starts from F203's fixed beta-two coefficient

\[
 A(N)=\sum_{d\mid N}d\chi_4(d),
\]

whose exact evaluation factors every distinct odd semiprime. F204 proved that
the binary q-product norm leaves every odd logarithmic coefficient free,
that scalar binary-orbit modular monomials delete the selector, and that no
fixed linear base-two Mahler equation or fixed root norm fast-forwards it.
F204 explicitly left vector-valued and nonholomorphic completions open.

F206 tests that next route without searching for a published factoring
solution. The new ingredients are:

1. the exact two-cusp radial powers of `P`, `P/P(-q)`, and `L`;
2. the semisimple fixed-projection cusp lemma;
3. the transposed divisor coefficient `A^vee`;
4. the exact Mellin reflection matrix with its tangent and cotangent factors;
5. the finite rational-normalization obstruction; and
6. the same-index circularity of the natural Whittaker repair.

The proof uses standard primary identities only: the q-gamma limit, Mellin
inversion, the Riemann zeta functional equation, the primitive `chi_4`
Dirichlet L-function functional equation, and the Gamma reflection and
duplication identities. All new conclusions are derived in the proof.

No mathematical computation was run. No web search for a factoring solution
was run. Hashing is used only to freeze the packet. No durable registry,
proved ledger, failed ledger, or progress ledger was edited by this artifact's
author.
