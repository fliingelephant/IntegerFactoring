# Fresh hostile re-audit of F200 V2

## Strict verdict

**PASS.** The V2 packet is authenticated. The two defects that forced the V1
FAIL are repaired exactly. A fresh hostile attack found no remaining
mathematical error within the explicitly narrow scope of the statement.

This PASS is conditional on the promoted interfaces named in
`V2_MANIFEST.md`: P171, P173, P175, and the explicitly conditioned surviving
P160--P161 branch. It does not promote or independently re-prove those imported
premises. F200 V2 is not a factoring algorithm and is not a general circuit
lower bound.

No mathematical computation or web search was used.

## 1. Pre-read authentication

I read `V2_MANIFEST.md` first. Before opening any V2 packet input, I computed
the four embedded frozen hashes. All matched:

```text
1edaedf1e0121e4603b8502cfb2473de250366cdc4b35acb216c3be314c8b7e5  V2_STATEMENT.md
b573b44d35d577e4c567448092997923c0a83cdeda8a1a1cb89ec9e1cf03e30b  V2_PROOF.md
5acac4d4bac434199b25f4e71674eb82308b3d03bbffb9af12f57968d62f1215  V2_SELF_AUDIT.md
6a0dd42695efa1fb211094548c6452f7beeac815f3207aadb1b752568f22365b  V2_PROVENANCE.md
```

The manifest itself also matched the separately supplied expected hash:

```text
e6f67fd5986846741796a59c044f6d60ccf9e51a72ab3e8fd673ed0da1aa6086  V2_MANIFEST.md
```

I then opened `V2_PROVENANCE.md`. It recorded this expected hash for the
preserved V1 failure:

```text
24e36e3e2e5d0fee70b1d60b85678786254824b36240bbf351529a936fd8ad92  HOSTILE_AUDIT.md
```

I verified that hash before opening the V1 audit. It matched. Thus this
re-audit used the intended frozen V2 packet and the authenticated preserved V1
failure.

## 2. Exact repair of the V1 provenance defect

The V1 audit failed first because its observed statement and proof no longer
matched the hashes in its manifest. V2 does not revise that failed packet in
place. `V2_PROVENANCE.md` records the observed V1 hashes, explains the
post-freeze additions, and preserves the V1 files. The V2 statement, proof,
self-audit, and provenance now match their new frozen manifest exactly.

This is the required repair. The old hostile FAIL remains attached to V1. The
old blind reconstruction does not authenticate or verify V2. No stale status
has been transferred.

## 3. Exact repair of the cyclotomic branch and cap defect

The V1 cyclotomic paragraph omitted two conditions needed to invoke P161. V2
now states each condition explicitly:

1. It fixes a public integer-valued numerical-QP extension-degree cap
   \(D(n)\).
2. It invokes P160--P161 with a roughness parameter \(T\ge D(n)\).
3. It leaves a returned factor and a factored exact common-order state as
   separate exits.
4. It conditions the representation conclusion only on the surviving branch
   \(H=\gcd(w-1,N)=1\).
5. On that branch, it invokes the local-order conclusion at the same hidden
   rational prime \(p\): \(\operatorname{ord}_p(w)>T\).

The proof then uses the actual construction \(w=y^Q\),
\(y=4^{N^n}\), so \(w=2^E\) for a public exponent \(E\). In the cyclic group
generated by \(2\bmod p\),

\[
\operatorname{ord}_p(w)=
\operatorname{ord}_p(2^E)mid\operatorname{ord}_p(2).
\]

Therefore, on this branch only,

\[
\operatorname{ord}_p(2)
\ge\operatorname{ord}_p(w)>T\ge D(n).
\tag{1}
\]

If a finite extension \(K/\mathbb Q_2\) with residue degree \(f\) contains a
primitive \(p\)-th root of unity, prime-to-two torsion reduces injectively
into \(\mathbb F_{2^f}^{\times}\). Hence

\[
p\mid2^f-1,
\qquad
\operatorname{ord}_p(2)mid f,
\qquad
[K:\mathbb Q_2]\ge f\ge\operatorname{ord}_p(2).
\tag{2}
\]

Equations (1)--(2) exclude degree at most \(D(n)\). This is a strict bound:
the order is strictly above \(T\), while \(T\) is at least \(D(n)\). The
conclusion is conditioned on the correct P161 outcome and the correct hidden
prime. It makes no claim about the earlier exits, complex approximation, or an
implicit phase representation. The second V1 defect is therefore repaired
without overreach.

One implicit point is harmless: the “active phase” is a primitive
\(p\)-th root when its frequency is coprime to \(p\). The exact phase test in
the preceding paragraph assumes \(\gcd(m,N)=1\), which supplies this condition.
The degree statement itself correctly says “primitive \(p\)-th root.”

## 4. One-denominator delta, gcd orientation, and floor sign

Balance gives

\[
p\le B<q,
\qquad
B<2p.
\]

Thus \(p\) is the only index in \([1,B]\) that is not coprime to \(N\). The
signed-binomial recurrence gives

\[
j(A_j-A_{j-1})=-NA_{j-1},
\qquad
x_j=\frac{A_{j-1}-A_j}{N}.
\]

For \(j\ne p\), Euclid's lemma makes \(x_j\) integral. At \(j=p\), the
imported P171 jump \(A_{p-1}\equiv1\pmod N\) gives

\[
x_p\in\mathbb Z+\frac1p.
\]

The exceptional sign is positive. Hence an active subset satisfies

\[
D_I=NS_I\equiv q\pmod N,
\]

so its endpoint gcd is \(q\). Its index-product gcd is \(p\). In the inactive
case these gcds are \(N\) and \(1\), respectively. The interval formula

\[
S_{[a,b]}=\frac{A_{a-1}-A_b}{N}
\]

telescopes with the stated sign.

At the root,

\[
S_{[1,B]}=\frac{1-A_B}{N}=\frac1p-h.
\]

Since \(0<1/p<1\), its ordinary floor is exactly \(-h\). The ordinary-floor
sign and the distinction from 2-adic evaluation are correct.

## 5. Quotient-valued transforms and correlation

Modulo \(\mathbb Z\), the padded signal is the point mass
\(p^{-1}\delta_p\).

- Every unnormalized Walsh coefficient is \(\pm1/p\), which is nonzero in
  \(\mathbb Q/\mathbb Z\).
- Every Fourier coefficient is
  \(\zeta_M^{\pm sp}/p+\mathcal O_K\). It is nonzero because a root of unity
  is an algebraic unit and \(1/p\notin\mathcal O_K\).
- At each Haar wavelet scale, exactly one wavelet contains the support point.
  Its coefficient is \(\pm1/p\). The root scaling coefficient is separate and
  does not contradict the wavelet count.
- A cyclic difference multiplies the Fourier coefficient by
  \(\zeta_M^{sh}-1\), up to convention. For dyadic \(M\) and odd \(h\), this
  factor vanishes only at zero frequency. Iterated odd differences therefore
  retain all nonzero frequencies.

The Fourier target is the additive quotient group \(K/\mathcal O_K\), not a
quotient ring. The transform remains well-defined because changing a rational
lift by an integer changes its coefficient by an algebraic integer.

For the rational lift, the displayed autocorrelation is

\[
\sum_jv_jv_{j+h}=p^{-2}\mathbf1_{h=0}.
\]

Thus the formula uses the unnormalized cyclic correlation. If “averaged” is
defined with an additional factor \(1/M\), the scalar would also have that
factor. This is a convention clarification, not a defect in the proof, which
displays and uses the unnormalized sum. Location independence remains exact.

## 6. Reciprocal cells, the tree gauge, and nonempty lifts

Inversion permutes the odd classes modulo \(2^t\), so

\[
j^{-1}\equiv a\pmod{2^t}
\iff
j\equiv a^{-1}\pmod{2^t}.
\]

Only \(j=p\) contributes in \(\mathbb Q/\mathbb Z\). The cell-sum word is
therefore a delta at \(a=p^{-1}\), and any bijective Boolean labelling merely
permutes that delta. Its Walsh transform remains full. “Labelling” must be
read as a bijection onto the Boolean cube, as the statement's identification
requires.

At every binary-tree fork,

\[
s_v=s_{v_0}+s_{v_1},
\qquad
\mathbf1_{\ell\in v}
=\mathbf1_{\ell\in v_0}+\mathbf1_{\ell\in v_1}.
\]

Subtracting \(u\) times the second identity from the first preserves every
fork equation for every proposed leaf. Each finite residue has an integer
representative. Representatives can be chosen at leaves and summed upward if
exact integer additivity is desired. Only the true pair produces the actual
floors; false gauges need not satisfy their sizes or values. The statement
does not claim otherwise.

For a next one-bit reciprocal lift, inversion gives two odd index classes
modulo \(2^{t+1}\). P175's sub-quarter range has
\(2^{t+1}=O(N^{1/4})\). The public balanced interval has length
\(\Theta(\sqrt N)\), which exceeds the modulus for all sufficiently large
inputs. It contains a representative of each class. Thus both lifted cells
are nonempty, and cell emptiness cannot break the additive gauge. This claim
is asymptotic and explicitly says so.

An invertible Haar or Walsh change of coordinates preserves the additive
solution family. It cannot select a gauge. This does not cover a nonlinear or
Archimedean constraint.

## 7. Endpoint accounting, run count, and sampling

Direct collection of terms gives

\[
N\sum_{j=1}^Bw_jx_j
=w_1A_0+
\sum_{j=1}^{B-1}(w_{j+1}-w_j)A_j-w_BA_B.
\]

The endpoint signs are correct. A weight with \(R\) nonzero adjacent
differences has at most \(R+2\) displayed terms. The imported P173 interface
evaluates a supplied numerical-QP set of endpoints modulo \(2^t\), not their
ordinary floors or exact rational support.

A residue cell is one arithmetic progression of spacing \(2^t\). Its Boolean
indicator has

\[
\frac B{2^t}+O(1)
\]

isolated runs. At

\[
t=\left\lfloor\frac{log_2N}{4}\right\rfloor-L(n),
\qquad L(n)=(\log n)^{O(1)},
\]

this is

\[
N^{1/4}2^{L(n)+O(1)},
\]

which is exponential in the bit length. The adjacent-change count differs by
only a constant factor. Literal endpoint materialization is not numerical QP.

The active cell has exactly one exceptional index. Uniform sampling hits it
with probability

\[
\Theta(2^t/B)
=N^{-1/4}2^{-L(n)+O(1)}.
\]

Its expected trial count is exponential. These are method-accounting results,
not circuit lower bounds.

## 8. Auxiliary rings, 2-adic continuity, and phases

On \(\gcd(M,N)=1\), localization gives

\[
\mathbb Z[1/N]\longrightarrow\mathbb Z/M\mathbb Z,
\]

and sends \(1/p\) to the ordinary residue \(p^{-1}\). Every residue is also
represented by an integer, so a predicate of that residue alone cannot
separate integers from integers plus \(1/p\). For a finite family of moduli,
the same conclusion follows from a compatible representative modulo their
least common multiple. The claim excludes the branch where \(N\) is not a
unit and does not silently apply localization there.

At powers of two, finite surjectivity gives the same result. At infinite
precision, \(\mathbb Z\) is dense in \(\mathbb Z_2\), so a continuous
Hausdorff-valued function constant on all ordinary integers is constant on
\(\mathbb Z_2\). This covers value-local convergent Mahler expansions and
finite-precision characters. It does not cover a discontinuous floor or an
algorithm that also reads a succinct rational presentation and \(N\).

For \(|m|\le Q(n)\),

\[
|e^{2\pi im/p}-1|\le2\pi Q(n)/p=2^{-\Omega(n)},
\]

because balance makes \(p=2^{\Theta(n)}\). Thus bounded numerical-QP
frequency has no inverse-QP signal. If \(m\) is uniform modulo \(N\), its
residue modulo \(p\) is exactly uniform, so a constant fraction of active
phases have constant separation.

Finally,

\[
e^{2\pi imS_I}=e^{2\pi imD_I/N}.
\]

For \(\gcd(m,N)=1\), this phase equals one exactly when \(N\mid D_I\), or
equivalently when the cell is inactive. An exact phase evaluator on adaptive
dyadic cells would therefore find \(p\). This is a reduction showing that the
missing evaluator is factor-bearing. It is not a construction of one, and it
does not equate approximate estimation with exact equality testing.

## 9. Final scope attack

The conclusions survive only in their stated domains:

- The harmonic event is exact support or exact rational integrality. Efficient
  finite-residue evaluation does not decide it.
- Haar sparsity identifies a formal path structure, not the hidden active path.
- The tree symmetry is additive. Actual floors, magnitude constraints, and
  nonlinear integer-part statistics are deliberately left open.
- Exponential run and sampling counts apply to the literal evaluators named in
  the theorem. They are not lower bounds for implicit circuits.
- The auxiliary-ring and Mahler conclusions are value-local and continuous.
  They do not cover the rational representation plus an Archimedean operation.
- Low-frequency complex phases have exponentially small separation. Exact
  high-frequency evaluation is only reduced to factoring, not ruled out.
- The local cyclotomic conclusion is an explicit finite-extension degree bound
  on the conditioned surviving P161 branch. It does not cover the other exits,
  complex approximations, or implicit encodings.

No statement in V2 resolves factoring, excludes adaptive prefix-cell
syndromes, or proves a general arithmetic-circuit lower bound.

## Result

**PASS.** V2 exactly repairs both V1 defects, and all other claims survive the
fresh hostile re-audit within their declared scopes.

