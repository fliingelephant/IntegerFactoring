# F227 provenance

The root task requested a kill-first investigation of a new Las Vegas source
for the beta-two plus aggregate-order framework:

- sample `x` from the certified factor arithmetic progression;
- recursively factor the smaller exponent `x-1`;
- use a unit return to split `N` or certify new common primary support.

Closest prior results checked before the proof were:

- P165/F187: one fixed exponent `N-1` with uniform bases;
- P194/F224: factor-cell sampling through Fermat and modified-AKS values;
- the F220 V2 primary-certificate and aggregate-terminal packet.

The new observation is the AP mean bound for
`gcd(x-1,r-1)/(r-1)`. It turns the complete factorization of `x-1` into a
clean probability boundary. The fixed-base return progression then gives
the complementary positive residual-order criterion.

No computation, public-web search, external theorem search, or remote run
was used. The only standard asymptotic input is the elementary divisor bound
`tau(m)=m^{o(1)}`, for which the proof packet includes a derivation.
