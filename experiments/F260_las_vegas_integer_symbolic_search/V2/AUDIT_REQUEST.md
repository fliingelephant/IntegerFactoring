# F260-D02 V2 fresh hostile pre-run audit request

Audit the exact frozen F260-D02 packet from first principles. This must be a
fresh independent review. Do not reuse the D01 audit verdict.

## Frozen inputs before manifest assembly

```text
d0ceeddbab866cc9d35f85565ffc0f3282a955fd22603d666e9d00790dd00229  ALGEBRA.md
2ebd75dea01d34fe72682d45524abc2e6072618866393d963ba6dccd221059d7  PREREGISTRATION.md
c6d0f016e471911c115a497311ef05e5682c796df6c3887626ac5cb25bcb3911  search.cpp
6812855a4ef7b87c351cfe1c29d098dae0470788139d9ca27b7c8d94f5de43be  remote_run.sh
b8f019b4e9b78cec7fcd772fb75ca7395c881ae6793db711bc78fe14991d9c59  PROVENANCE.md
34baff67c8af30225206e32d8009c3be1dee06d405532bd6857baf82e14a4c6f  VALIDATION_PENDING.md
297f87601eaab098cb18cc6244543bbd7c80c390b2ba575879f80e41f3ec04a3  STATIC_REVIEW.md
```

Authenticate all final files through `FROZEN.sha256`. Also authenticate that
file itself and record its SHA-256.

## Required hostile scope

Audit all of these boundaries:

1. the exact annihilator return/order laws, direct and stripping factor
   probabilities, strict growth, factor-or-growth, and the linear expected
   log-gain identity;
2. `M=1`, universal `M=2`, hidden-gcd separation, and all three exact
   integer dyadic distributions;
3. P205, `H`, residual logs, baseline change, distinct-value ordered-pair
   Miller semantics, hard-residual collision mass, and direct P241 joint
   averaging;
4. every common, transformed, pair, factored-support, recursive-oracle, and
   saturation gcd boundary and its certificate output;
5. all 22 source bases, exact-value thinning, typed syntax ordering, public
   fingerprints, type caps, hypergeometric identity-only boundary, and
   nonmaterialized control;
6. unconditional factored programs versus nonoperational recursive-oracle
   diagnostics in ranking, selection, held-out, output, and lead logic;
7. bounded deterministic cohort generation, exact counts, balance,
   primality, safe-safe/consecutive structure, retries, and cross-split
   modulus distinctness;
8. the four complete discovery orders, literal top-eight union, order-one
   fill, byte-authenticated 32-row selector, byte-authenticated public corpus,
   and the train/held-out data firewall;
9. all per-size aggregates, exact expected-certificate mass, four-size lead
   gates, loss definition, collision-anomaly gates, and null-result behavior;
10. overwrite, stream, one-GiB, exact-file, row/field, phase-manifest,
    audit-binding, resource, conflict, shared-deadline, benchmark, and final
    source-hash runner gates; and
11. whether `VALIDATION_PENDING.md` accurately withholds compilation,
    execution, runtime, memory, and target qualification.

The quadratic D01 primary loop and the exact D01 selection drift are
decisive regression checks. Inspect the operational cost of the final smooth
annihilator path, not only the closed-form formula.

## Audit constraints and output

F258-D01 is active. Do not compile or execute `search.cpp`. Do not invoke or
parse-run `remote_run.sh`. Do not use a remote host. Do not generate, inspect,
or score a tiny or full cohort. Do not edit a frozen file or a durable ledger.

Write only new audit artifacts:

```text
HOSTILE_PRERUN_AUDIT.md
HOSTILE_PRERUN_AUDIT.sha256
```

If and only if there is no blocker, the audit must contain these two exact
standalone lines:

```text
Verdict: **PASS — CLEARED FOR LAUNCH**
FROZEN_SHA256=<lowercase SHA-256 of the exact FROZEN.sha256 bytes>
```

The sidecar must contain one standard `sha256sum` record for
`HOSTILE_PRERUN_AUDIT.md`. A failure must use an explicit FAIL verdict and
must not contain the PASS line. The audit cannot claim compile, self-test,
benchmark, runtime, memory, or cohort evidence while validation is pending.
