# F265-D18 fresh hostile theory/interface audit — PASS

## Verdict

**PASS.** I found no remaining output-name ownership conflict, RATE_IO
chronology ambiguity, transient-byte error, counter/evidence leak, preflight
interval contradiction, final-membership conflict, role-composition defect,
root-construction ambiguity, mathematical change, or authorization expansion
in the authenticated D09--D18 additive composition.

D18 closes the inherited RATE_IO source contract. The public preflight role
owns exactly one ephemeral final basename and its conventional temporary. Each
of four serial repetitions starts and ends with both names absent, creates one
payload inode exclusively, writes and authenticates exactly 67,108,864 bytes,
renames that same inode, authenticates it again under the final name, unlinks
it, synchronizes the directory, and commits evidence only after final absence
proofs. The retained preflight result files are created only after the D15
interval ends.

This PASS authorizes only source drafting for the four D18 roles. It does not
authorize source freezing, a source PASS, compilation, fixture
materialization, fixture verification, preflight, evaluation, replay,
sealing, diagnostics, private classification, packaging, result release,
remote execution, or a ledger edit. Approved hard dynamic containment remains
a mandatory later gate for every executable mode.

## Authenticated composition

| Artifact | SHA-256 |
|---|---|
| `D09_DRAFT_ALGEBRA.md` | `93ee6021b969a3624cf499df5e18ea9c262e2a095a6b0634644cc1d69ee8030d` |
| `D09_DRAFT_PREREGISTRATION.md` | `3b499578678a0f71c75f52608e53f2fba7f390fb5a09a95bc20c61d09fe1c0a4` |
| `D10_DRAFT_ALGEBRA.md` | `aab2397c601b645f71da0bc79a8f58ed9ef79ea984fa7f7458ab2a49582a17a7` |
| `D10_DRAFT_PREREGISTRATION.md` | `be7e2a05a1eac6659000cdfe71469697d76b867b42cb60c1c244b06ec63afb6e` |
| `D11_DRAFT_ALGEBRA.md` | `a177438e425a20550342e7efa94ec1f70d30f40fe204640f983435dc36554f27` |
| `D11_DRAFT_PREREGISTRATION.md` | `9b477799e006a9b2bb49eebcf062917f1a3c91a9852ce8fbe825fcea1a1df23e` |
| `D11_HOSTILE_THEORY_AUDIT.md` | `777ccd163959a7ec4e14bdcd3c9d12fc1d58732207f12520806e1ad2a2705246` |
| `D12_DRAFT_ALGEBRA.md` | `eae660da5a5c5344b83ccd99301cfb4f2db89d5c259cf73194f85892e2d7dcb0` |
| `D12_DRAFT_PREREGISTRATION.md` | `65682542478a2c2270f3657d2c741ea253445ad3cf160758449a0ce21e806ecd` |
| `D13_DRAFT_ALGEBRA.md` | `915ec124f6be79a1530af3df628f8c65eaa5bde845f073f67f5077e66258ea9c` |
| `D13_DRAFT_PREREGISTRATION.md` | `42667461ac3a21820f3ea1d5de545d17ff03c719a38cd405dbdb0e91ff083c17` |
| `D14_DRAFT_ALGEBRA.md` | `42690a13d5a0fade1dd644bf29d299d9bab3b5cdd6a68d08354f1e9a5c6fb4f6` |
| `D14_DRAFT_PREREGISTRATION.md` | `4b02e30cfe2986fbc04dbf8c4faf22743c18acb1a64d2a07b30eefe2c6338b9b` |
| `D14_HOSTILE_THEORY_AUDIT.md` | `b7067c104eaf96dfa81d7bd08dcc9326e7616016fb66202c078bf1c2ea5ef03e` |
| `D15_DRAFT_ALGEBRA.md` | `563293afda1cf4d183b4d70ec30da60d0339086a52d9395df8b4d70d1e7ceeb6` |
| `D15_DRAFT_PREREGISTRATION.md` | `9c3b7cc4d855e906d8c9f35bcf7846631d28d728138df07f0e812cf59c12798d` |
| `D16_DRAFT_ALGEBRA.md` | `10d0e74462aac944a32dbc68404aa30c730d1f5b919f1fb4dd9e5a76ae92f5b2` |
| `D16_DRAFT_PREREGISTRATION.md` | `8a27d1d3af5b6f46d5f90e2f3545928cde55932c8012c6829b7c67207bd556a2` |
| `D17_DRAFT_ALGEBRA.md` | `e784dfb9d151e816ef43644e480e705c7414d4af34114250da7d399c319d46d3` |
| `D17_DRAFT_PREREGISTRATION.md` | `acd92da342f2f88667a6b3cbfc3be1c13e9f8fdeabf4789020f3472319ed62d3` |
| `D17_HOSTILE_THEORY_AUDIT.md` | `fc068523f2d45f1a853aac1db860fc64ccb66f6ffbf8f8f8b372a2ed3937f638` |
| `D18_DRAFT_ALGEBRA.md` | `6a163bb4a642760f41a5d0fab0b5bbea40e936eef2dff93921b65fe7c7ce815d` |
| `D18_DRAFT_PREREGISTRATION.md` | `944b0ffce0036f2970cb56e097735622b1aaf97f186a40673d812d244f3709f6` |

All twenty-three hashes matched the files reviewed. I authenticated both D18
drafts before reading them, then authenticated and read all twenty-one imported
predecessor and hostile-PASS bytes in full. The D11, D14, and D17 PASS files
remain evidence only for their exact authenticated compositions. I did not
transfer a predecessor verdict to D18.

## Closed name ownership and deterministic bytes

Only `f265_d18_public --mode preflight` may create these descriptor-relative
basenames in its already validated output root:

```text
F265-D18.RATE_IO.bin
.F265-D18.RATE_IO.bin.tmp
```

The second name is exactly D13's `.X.tmp` temporary for the first name. Both
are fixed source constants. No CLI argument, environment value, current
working directory, input root, alternate writable root, unnamed path, or
implementation-selected basename can rebind them. The D18 exception is narrow:
the names are legal only during RATE_IO in preflight and are not retained
result files, manifest lines, root-identity inputs, fixture payloads, corpus
inputs, private inputs, or finite-label evidence. Every other mode continues
to forbid them.

The authenticated `IO_BYTES` operand is total. `arg0=4000000` is hexadecimal
and equals 67,108,864. Canonical `arg1` registers one byte. The logical and
physical file is exactly 67,108,864 copies of that byte, with no header,
newline, checksum trailer, sparse hole, zero-fill shortcut, compression, or
formula substitute. The materializer records the full-stream SHA-256 and
`BYTE_COUNT=67108864`; the independently authored verifier reconstructs the
stream; public preflight hashes the actual file and requires that authenticated
digest. These are the correct materializer, verifier, and production roles.

## Exact four-repetition lifecycle

The four repetitions are serial and their timers do not overlap. Operand and
expected-semantic authentication, fixed-buffer reservation, and independent
expected-digest construction occur before the first repetition timer. Each
repetition resets its local counter and hash state and follows this closed
sequence:

1. Before timing, enumerate the retained output-root descriptor and require
   empty membership. Independently probe both ephemeral names no-follow and
   require `ENOENT`.
2. Start the monotonic timer immediately before exclusive creation of
   `.F265-D18.RATE_IO.bin.tmp` with
   `O_CREAT|O_EXCL|O_NOFOLLOW|O_CLOEXEC`, write-only access, requested mode
   `0600`, and inherited `umask 077`.
3. Require a regular, one-link, effective-UID-owned `0600` file on the output
   root's device. Record its device and inode. Write exactly 67,108,864 bytes
   through the production writer, complete partial writes in order, reject a
   zero or failed write, and forbid the counter from crossing the exact bound.
4. Require exact offset and `IO_BYTES=67108864`, `fsync` the payload descriptor,
   verify identity and exact size with `fstat`, and require successful close.
5. Reopen the temporary descriptor-relative and no-follow. Require the same
   authoritative identity, hash exactly the complete bytes, reject early EOF
   and an extra byte, require the authenticated digest, require identity
   stability across the read, and close successfully.
6. Re-probe the final basename and require `ENOENT`. Rename within the same
   retained output descriptor, require success, and `fsync` that directory.
   Require the temporary absent and the final name to identify the same regular,
   one-link, owned, mode-`0600`, exact-size inode.
7. Open the final descriptor-relative and no-follow. Hash exactly the complete
   bytes again, require stable authoritative identity and the authenticated
   digest, reject early EOF and an extra byte, and close successfully.
8. Unlink only the final basename through the retained output descriptor and
   `fsync` the directory. Require empty membership and independent `ENOENT`
   results for both names.
9. Stop the timer after the absence proofs. Apply D15's conservative integer-
   nanosecond ceiling and only then atomically commit that repetition's elapsed
   value, exact counter tuple, and digest match in memory.

Thus close precedes rename, the inherited temporary verification precedes
rename, the final hash follows rename and directory synchronization, and
unlink plus directory synchronization precede the timer stop. The registered
rate remains the maximum of four complete integer-nanosecond durations divided
by 67,108,864. Each repetition independently requires the one-repetition
counter tuple; the expected counter is not multiplied by four.

## One inode, byte peaks, and failure evidence

Exclusive creation starts one regular one-link payload inode. Before
`renameat`, only the temporary basename names it. A successful same-directory
rename changes that name; it does not copy the payload or add a link. After
rename, only the final basename names the same recorded device/inode pair.
Both read passes close before unlink. The four repetitions unlink serially
before the next starts.

The exact transient payload peak is therefore

```text
1 * 67,108,864 = 67,108,864 bytes.
```

This is below the unchanged 134,217,728-byte per-file cap. It adds no retained
byte. The inherited directory calculation remains exact:

```text
226,088,448 + 64*262,400 + 67,108,864 = 309,990,912 bytes.
```

D18 correctly replaces only D10's ambiguous two-name coexistence sentence;
it changes no retained-output category, projection coefficient, worker
temporary, memory limit, or operation limit.

Every partial write, flush, close, identity, rename, hash, unlink, directory-
sync, membership, absence, cleanup, counter, or timing failure is fatal to
preflight. A failed or partial repetition contributes no elapsed sample, rate,
projection term, counter tuple, digest evidence, PASS row, or success manifest.
On a caught post-create failure, cleanup closes owned descriptors and unlinks
only a name proven no-follow to identify this repetition's inode, temporary
first and final second, then synchronizes the directory and proves both names
absent. Cleanup success does not resume preflight. If in-process cleanup is
prevented or cannot establish absence, no output root is releasable; a later
separately authorized runner must discard that complete unreleased root.

## D15 interval, final membership, and role closure

D18 targets the actual inherited contradiction. D12/D13 formerly allowed a
mode to create only its assigned fixed names, while D15 put all output-root
creation after an interval that itself contained RATE_IO fixture execution.
D18 assigns legal fixed ephemeral names to public preflight and replaces only
D15's output-creation sentence: retained preflight-result temporaries remain
after the interval, while the complete RATE_IO lifecycle occurs inside it.

All four repetitions finish, unlink, synchronize, and prove absence before the
remaining projection and final retained-input checks finish. Immediately before
the D15 end timestamp, the output root is enumerated and required empty. The
timestamp is taken only after every non-duration field and final input check is
ready. Only afterward may `.F265-D18.preflight.tsv.tmp` open. At successful
process exit, exact `BYTEWISE` membership is:

```text
F265-D18.PREFLIGHT.sha256
F265-D18.preflight.tsv
```

Both RATE_IO names are independently absent. They enter neither manifest nor
root preimage and contribute zero to D15's twelve-file `preflight_bytes` input
footprint. The original output-root identity tuple and all inherited
input/output nonaliasing checks remain mandatory.

Materializer and verifier create no RATE_IO transient. They materialize and
independently verify only its authenticated operand and expected semantics.
Evaluate, replay, seal-public, diagnostics, and classify cannot create either
name. Replay continues to reconstruct scientific evaluation evidence; it does
not replay preflight rate fixtures. D18 therefore composes with the four source
roles, eight modes, ten invocation grammars, private firewall, and source-token
transition without adding an option, writable root, process, role, or artifact.

I found no other surviving exact source-interface contradiction in the full
composition.

## Roots, mathematics, containment, and review boundary

The D18 theory root has exactly twenty-three LF-terminated lines. Lines 1--21
are the D09--D17 predecessor and hostile-audit hashes in the D18 algebra table's
displayed order. Line 22 is the authenticated D18 algebra hash. Line 23 is the
authenticated D18 preregistration hash. Every line is basename, tab, hash, LF.
There is no self-reference and no D18 audit line in the root.

The future-only substitutions are complete and length-safe:

```text
F265-D17  -> F265-D18
f265_d17_ -> f265_d18_
F265-D17. -> F265-D18.
F265_D17_ -> F265_D18_
```

They change no predecessor byte. `F265-D18` remains eight bytes. The public and
private sources retain one occurrence each of both invalid D18 fixture-root
tokens, four occurrences total, with the inherited one-edit replacement and
fresh source-audit gate.

D18 changes no elliptic source, random stream, factor chronology, peel, F271
operation, kernel, basis, root class, final hit, packet ledger, rate denominator,
projection formula, duration encoding, finite decision, diagnostic, private
firewall, theorem boundary, or result scope. It supplies no containment
fallback or execution authorization.

I used only read-only SHA-256 authentication and full-text inspection before
creating this report. I did not inspect private labels, modify a draft or
predecessor, create C++ source, a runner, or fixture bytes, compile, invoke
preflight, execute an evaluator, use a remote host, touch a ledger, stage, or
commit. This audit is outside the normative D18 composition. Its SHA-256 is
reported after sealing; a self-hash is not embedded in these bytes.
