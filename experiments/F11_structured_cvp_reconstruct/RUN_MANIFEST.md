# F11 reconstruction manifest

## Scope

* Family: `F11_structured_cvp_reconstruct`.
* Date: 2026-08-06, Asia/Hong_Kong.
* Input: the corrected bare statement supplied by the root agent, the project
  `AGENTS.md`, and the status/computation rules already read from `PROMPT.md`.
* Output: `RESULT.md` and this manifest, both confined to the fresh
  `experiments/F11_structured_cvp_reconstruct/` directory.

## Blindness and exclusions

No canonical proof, failure, registry, or progress file was read.  Neither
`experiments/F11_structured_cvp_kill/` nor
`experiments/F11_structured_cvp_audit/` was listed, opened, searched, or used.
No canonical file was edited.

## Method and provenance

This was a proof-only reconstruction from the bare statement and first
principles.  It used no computation, inline code, external source, web search,
subagent, benchmark, or hidden factorization call.  Consequently there are no
computational logs, timeouts, output tables, or failure dispositions to
record.  The proof explicitly audits the final carry, carry digit bound,
integer-normal-form bit complexity, rational/integer CVP conventions, distance
gap, recursive reduction, incidence minor, codimension, and the hand-checkable
`N=25` certificate.

## Status

All corrected bare assertions are reconstructed as proved in `RESULT.md`.
The honest artifact status is **self-audited**.  It has not received a fresh
hostile audit, cross-family audit, or human review within this reconstruction.

