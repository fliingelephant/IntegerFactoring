# F148 V3 preregistration — nondegenerate deduplicated certificate

Use the V2 frozen corpus, loop order, and corrected evaluator. Add these
requirements:

1. neither canonical endpoint is self-inverse;
2. none of the four individual exact relation values is an integer square;
3. neither two-column cycle product is an integer square;
4. only the union of the two cycles is required to be a square.

Stop at the first certificate meeting all conditions. Preserve V1 and V2 as
rejected discovery outputs.
