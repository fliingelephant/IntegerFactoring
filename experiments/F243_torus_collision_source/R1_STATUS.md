# F243-R1 status — implementation smoke only

After freezing `PREREGISTRATION_R1.md`, the verifier was compiled and run on
the first five R1 inputs on the local Mac.  The aggregate smoke report was
viewed.  This violated R1's remote-only execution clause.  R1 is therefore
not a confirmatory numerical run and none of its values are evidence for a
claim.

The code defect was procedural, not arithmetic.  Preserve this record.  Use
the disjoint, frozen F243-R2 input set for the official remote scan.
