# F107 failed runs

## R01 — valid preliminary classification

R01 completed and gave the same base, metric, and carry classifications used
in R02. It did not print the two intersections that exclude carry rows from
the metric categories. The valid preliminary output and log are preserved as
`OUTPUT_R01.json` and `RUN_R01.log`. R02 adds those diagnostic categories and
is authoritative.
