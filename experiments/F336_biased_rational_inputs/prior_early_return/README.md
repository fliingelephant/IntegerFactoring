# Earlier early-return screen convention

These original files preserve the first exact small-input comparison. It
stopped after the first successful child gcd. Frozen F334/F336 instead
charge both positive child gcds before returning their factor set.

Success probabilities, query counts and reachable paths agree. Gcd costs
belong to different policies. The active files one directory above use
the F334/F336 convention; the implementation repair does not change the
frozen F334/F336 sources or their numerical evidence.
