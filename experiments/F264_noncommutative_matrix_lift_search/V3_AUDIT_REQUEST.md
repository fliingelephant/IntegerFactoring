# F264-D03 V3 fresh hostile pre-run audit request

Authenticate every path in `V3_FROZEN.sha256` before reading it. Authenticate
the preserved V1 and V2 manifests and both hostile FAIL audits. Treat every V1
and V2 byte as immutable history.

Use static inspection only while an incompatible F258--F263 process is active.
Do not compile, execute a C++ mode, generate a cohort, or open prior cohort
output. Audit these claims from first principles:

1. V3 changes no algebraic formula, public generator, source seed, cohort,
   family, candidate expression, score, threshold, or held-out rule from V2.
2. The order scope has exactly eight main words and one cyclic control in each
   of two profiles. Both factored schedules run on all 18 matrices, for exactly
   36 trials per input. No text still describes this as a per-profile 16+2 cap.
3. Every one of those 36 trials evaluates all 14 ordered original/conjugate
   proposals before any order-path return. The evaluator does not short-circuit
   on a unit root, a proper gcd, a failed initial return, or an order-strip gcd.
4. Each row records 504 total checks, 36 checks for each proposal index, match
   totals, and a transcript hash that includes the proposal residue, square
   residual, gcd outcome, proposal index, and trial order. The self-test freezes
   the same invariants.
5. Split certification and proper-gcd precedence remain mathematically valid.
   The exhaustive evaluation must not turn a nonsquare proposal into a split
   certificate or change the exact order-stripping theorem.
6. The row validator reconstructs and compares the exact ordered 2,560-column
   header. It also checks exact anomaly headers, row cells, split labels, cohort
   sizes, primality constraints, noncommutativity count, 36/504 root counts,
   and all 14 per-index counts.
7. JSON validation uses exact top-level and nested key sets. It checks the ten
   exact ordered decoy names and positive counts, all 30 family names, exact
   cohort/family/P205 cell order and counts, exact candidate count and IDs, and
   aggregate root counts.
8. Checksum authentication, self-test/benchmark/description parsing, both
   report validators, compression, manifest creation, and the final output
   gate all run through the same firewall, resource gate, 4 GiB cap, `nice 15`,
   two-second owned-byte monitor, and remaining shared four-hour deadline.
9. Manifest creation finishes before the final monitored aggregate-size gate.
   The final gate includes its own logs through the post-child byte check. No
   owned file is created after that gate.
10. No hidden label or discovery result selects a public source, scope,
    proposal, candidate, order trial, or held-out threshold.

Return **PASS** only if no blocker remains. A PASS authorizes target validation
only after every F258--F263 incompatible process is absent and target resource
gates pass. Preserve the audit as a new file. Do not edit any V1/V2/V3 frozen
artifact or durable ledger.
