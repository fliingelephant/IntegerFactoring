# F260-D02 V2 static review

Review type: manual source and document inspection only.

No compiler, parser execution mode, binary, runner, corpus generator, remote
host, or cohort was used. This record does not qualify target compilation,
runtime, memory, or output projections.

## Static result

The prepared packet has no known preregistration mismatch after the following
source-level checks:

- The factored score implements `D`, each primary equality law, stripping
  factor probability, strict growth, and expected log-lcm gain. The expected
  gain is `both * product(E) * sum(G/E)`. Each state uses one primary sum and
  no product-over-other-primaries loop.
- The two operational states are exactly `M=1` and `M=2`. The hidden gcd
  state is output only. Three dyadic distributions use integer `J`, capped
  lcm states, and exact `Delta-Phi` bins.
- P205 retains exact probability, `H`, residual logs, and baseline change.
  Pair scoring and collision denominators contain only distinct exact values.
  Collision support removes every primary already in `N-1`.
- The common scalar bank contains all registered raw public leaves. Every
  admitted transformed value and pair difference has a gcd screen. Exact
  factor and saturation diagnostics survive aggregation.
- The grammar has 22 source bases. Hypergeometric bases admit identity only.
  Exact-value thinning is deterministic. Sequence, word, and factored
  candidates use public 192-bit fingerprints and label-free caps.
- The selector takes exactly eight positions from each complete order before
  union. The held-out parser checks SHA-256, exact header, 11 fields, 32 unique
  IDs, kind, flags, fingerprint, numeric ranges, and syntax on the same bytes.
- Discovery-corpus parsing checks its digest, exact header, ordering, counts,
  canonical odd moduli, bit lengths, and global distinctness. Held-out starts
  only after both authenticated parses.
- All seven factor-size slots survive aggregation. The lead writer checks all
  four held-out sizes, the 40-to-60 upper-median loss change, four exact
  expected-mass cells, operational status, and all 12 collision-anomaly
  cells.
- Writers refuse overwrite, charge a one-GiB aggregate budget before each
  line, check stream status, and create byte/line/SHA phase manifests. The
  runner independently checks exact file sets, TSV dimensions, hashes,
  manifests, combined output and logs, resources, conflicts, and deadline.
- A delimiter-balance scan reported zero net raw braces, parentheses, and
  brackets in `search.cpp`. This is not a C++ syntax check.

## Pending evidence

Compilation, self-test, benchmark, target libraries, runtime projections, and
runner behavior remain unvalidated. An independent hostile audit must review
the exact frozen hashes. `VALIDATION_PENDING.md` controls launch qualification.
