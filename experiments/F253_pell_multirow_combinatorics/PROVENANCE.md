# F253 provenance

The parent task asked whether the fixed-past P214 estimate can control a
retrospective P66 parity kernel through matroid ordering, circuit pivots, or
extra Pell-orbit structure.

The first derivation was the class-fibre argument:

\[
m\le B_\Sigma(2^r-1).
\]

The labelled-copy vector-space example then showed that this rank bound is
optimal from the P214 fibre information alone. Counting a marked member of
every zero-sum subset gave the all-subset bound in the packet.

A small Pell-form identity,

\[
(1+2\cdot1^2)(1+2\cdot4^2)=1+2\cdot7^2,
\]

suggested testing whether one fundamental-unit orbit contains analogous
relations after canonical reduction. The local resources were inspected.
V1 was preregistered, its source was frozen, and it found a useful relation
only after earlier cleanup factors. V2 was then separately preregistered
with a screen-free filter and found the \(N=4331\) global-root relation.

The identity was subsequently recognized as the odd-multiple Pell
polynomial law. The exact theorem explains the V2 witness:
\(51=3\cdot17\), \(F_{3,2}(6)=1746\), and \(G_{3,2}(6)=289\).

No remote computation, web search, external theorem lookup, factorization
oracle, or durable ledger edit was used. Trial division in the finite search
was verifier-only. The theorem uses the already promoted P214 count and
elementary binomial, linear-algebra, and Pell identities.

