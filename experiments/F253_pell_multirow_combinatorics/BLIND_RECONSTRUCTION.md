# Blind reconstruction of F253

Authenticated input SHA-256:
`a9b32bd8bfdc68da9fb789ffae7640d1ae504b4ba382363a80427626a75bf2a0`.
Only `STATEMENT.md` was used.

## Verdict: FAIL

The combinatorial results, Pell polynomial identities, carried identity, and
finite arithmetic certificate reconstruct. Theorem B nevertheless states a
dependency without ensuring that its alleged pair consists of two distinct
retained rows.

## Reconstruction

The fibre cap gives at most (B_\Sigma) rows for each of the (2^r-1)
nonzero classes. Hence

\[
m\le B_\Sigma(2^r-1),\quad
r\ge\left\lceil\log_2(1+m/B_\Sigma)\right\rceil,
\quad d\le m-r_{\min}.
\]

The cap alone permits (d=0). Double-counting a square (k)-subset by its
(k) deleted-row pivots gives

\[
kZ_k\le B_\Sigma {m\choose k-1},\qquad
\Pr_k(\text{square})\le {B_\Sigma\over m-k+1}.
\]

The kernel has (2^{m-r}) subsets, so a Bernoulli-(1/2) subset is square
with probability (2^{-r}\le B_\Sigma/(m+B_\Sigma)). Labelled copies of all
nonzero vectors in (\mathbb F_2^r) attain the rank bound.

For odd (k), binomial expansion and (X^2=1+DY^2) give integer
polynomials (F_{k,D},G_{k,D}) with

\[
(X+Y\sqrt D)^k=XG_{k,D}(Y)+F_{k,D}(Y)\sqrt D,
\]

and taking norms gives

\[
1+DF_{k,D}(Y)^2=(1+DY^2)G_{k,D}(Y)^2.
\]

If (F_{k,D}(y_j)<N), reduction of the Pell orbit gives
(y_{kj}=F_{k,D}(y_j)), hence
(A_{kj}=A_jG_{k,D}(y_j)^2). For a genuine distinct-row pair with unit
supplied roots, its normalized root is (+1). Once the missing distinctness
and retention hypotheses below are imposed, a kernel generated by such
pairs also has trivial normalized-root image, by multiplicativity.

For (k=3), reconstruction gives

\[
F_{3,D}(Y)=Y(3+4DY^2),\qquad G_{3,D}(Y)=1+4DY^2.
\]

Writing (F_{k,D}(y_j)=y_{kj}+cN) and expanding gives

\[
A_jG_{k,D}(y_j)^2-A_{kj}=DcN(2y_{kj}+cN).
\]

The certificate checks exactly:
(4331=61\cdot71), (F_{3,2}(6)=1746), (G_{3,2}(6)=289),
(6097033=73\cdot289^2), and
(73\cdot6097033=21097^2). Both supplied and exact roots are (3773)
modulo (4331), so the two gcds are (4331) and (1).

## Defect and minimal repair

Take the allowed value (k=1). Then

\[
F_{1,D}(Y)=Y,\qquad G_{1,D}(Y)=1,
\]

so (kj=j). The claimed “two rows” are one row. Multiplying that row by
itself is not a row-subset dependency: over (\mathbb F_2), its alleged pair
vector is (e_j+e_j=0), the empty dependency. Thus the theorem's dependency
and decoy conclusion does not follow as written. Modular orbit repetition can
also make (y_{kj}=y_j) for (k>1).

For every finite-bank dependency claim, require all of the following:

- (k>1);
- the index (kj) is in the bank's frozen window;
- (y_{kj}\ne y_j);
- both corresponding distinct rows remain in the bank after cleanup.

Keep the stated unit-root hypothesis. Under these conditions the pair is a
real kernel vector, and the generated-kernel conclusion is valid.
