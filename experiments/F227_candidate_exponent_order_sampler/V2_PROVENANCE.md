# F227 V2 provenance

## Version origin

F227 V1 investigated the following Las Vegas source for the beta-two plus
aggregate-order framework:

- sample `x` from the certified factor arithmetic progression;
- recursively factor the smaller exponent `x-1`; and
- use a unit return to split `N` or certify new common primary support.

Its hostile audit returned **FAIL** for one formal statement defect. V1 did
not define `n` or numerical QP, and it did not bind the QP constants in the
adaptive diffuseness claim uniformly over all histories. The audit
independently validated the AP mean, one-trial probability bound, fixed-base
progression, nonstale classification, rough-order corollary, and recursive
shape.

V1 and its hostile audit remain unchanged. V2 is a new proof-only packet.

## Exact V2 repair

V2 adds

\[
n=\lceil\log_2(N+1)\rceil
\]

and fixes one function

\[
\mathcal Q(n)=2^{C_*(\log_2(n+1))^{k_*}}
\]

with constants independent of the input, hidden factors, adaptive history,
and stage. The same function bounds both:

1. the relative largest atom `eta H` of every conditional candidate law;
2. the total number of adaptive trials.

Every trial base is explicitly conditionally independent and uniform after
the candidate and all public preprocessing are fixed. This produces one
uniform exponential constant for the per-stage estimate and makes the
adaptive union bound a closed theorem statement.

Theorems A, the exact one-trial part of B, C, and D are mathematically
unchanged. The asymptotic and recurrence claims are restated using the one
fixed envelope.

## Closest prior routes

- P165/F187 fixes the exponent `N-1` and samples uniform bases.
- P194/F224 samples the factor cell through Fermat and modified-AKS values.
- F220 supplies the primary certificate and aggregate terminal used after a
  global return.
- P161 supplies the rough-order premise used only in Corollary D.

The F227-specific observation remains the AP mean bound for
`gcd(x-1,r-1)/(r-1)` and its complementary fixed-base residual-order
criterion.

## Evidence and ledger policy

No mathematical computation, scripted experiment, finite search, random
sampling, remote run, web search, or numerical fit was performed. Hashing is
used only to authenticate V1 and freeze V2.

No durable registry, proved ledger, failed ledger, progress ledger,
statement ledger, inspiration file, or process-lessons file was edited.
