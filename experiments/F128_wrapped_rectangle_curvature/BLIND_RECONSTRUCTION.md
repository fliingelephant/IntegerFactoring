# F128 final proof-blind reconstruction — PASS

A fresh reader used only the corrected self-contained statement. It
independently reconstructed all four results.

- The six floor ranges give the four exact carry formulas and the curvature
  decomposition.
- The P114 base minor has rank three under the stated unit conditions. Its
  fourth-column rank jump is exactly the curvature residual modulo each
  prime factor.
- At most \(a^4b^4\) quotient patterns occur. Each non-global pattern has at
  most two roots modulo each hidden prime, giving the stated uniform-source
  density bound.
- Multiplication by \(L=sa_0b_0\) converts every signed-small canonical
  inverse into one bounded public scalar. The same threshold kills the
  endpoint and eligibility screens.
- For \(N\equiv-1\pmod{sab}\), all wrapped floor curvatures cancel when
  \(s>1\). The \(s=1\) correction gives
  \((b-a)(1-N)\) exactly.

No counterexample or additional caveat remained.
