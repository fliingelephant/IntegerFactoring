# F157-D01 — sparse section-feedback capability

## Registered question

On the five fixed frozen-layer nulls in `INPUT.json`, does the first new
F156 section-feedback layer find a direct factor?

Rebuild each frozen base ledger from the public `N` only. Build its complete
multiplicity-aware gcd-free parity basis. Keep the actual decorated lift of
each first-occurrence parity-basis record. Then test:

1. every support-one lift;
2. every unordered support-two lift.

The support-two search can use the disclosed factors only to index the
candidate pairs. Each reported hit must then be reconstructed from the
public factor-free blocks and verified by an ordinary gcd with `N`.

This is a fixed finite capability test. It does not test a frequency law or
an all-input factoring theorem.
