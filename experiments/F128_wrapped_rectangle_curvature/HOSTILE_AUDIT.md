# F128 hostile audit — PASS WITH CORRECTIONS

The hostile audit checked all four candidate claims. It found no false
identity. The final files include its exact scope corrections.

1. The wrap decomposition and local-rank criterion passed exactly.
2. The density theorem is only for fixed \(a,b\) and uniform \(u\). Both
   prime-only events must be counted. The final constant includes them.
3. The signed-small theorem needs one bound that dominates the curvature,
   endpoint, and eligibility scalars. The final statement uses
   \(6L^2(a_0+b_0+a_0b_0+1)\).
4. The fully wrapped trap passed. Eligibility explicitly requires
   \(a\ne b\) and sufficiently large hidden factors.

The audit emphasized that none of these results controls an adaptive
\(N\)-dependent selector that targets the rare uniform-source roots.
