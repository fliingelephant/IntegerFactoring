# F245 V3 provenance

- V1 is preserved byte-for-byte, including its hostile PASS and strict
  statement-only FAIL.
- V2 is preserved byte-for-byte, including its hostile PASS and strict
  statement-only FAIL.
- V3 responds only to the V2 blind finding.  It formalizes the restricted
  Las Vegas grammar, filtration, sampler chronology, coefficient screens,
  clean and nonclean branches, direct word gcds, two-primary chain, and
  exhaustive factor-return list.
- The inverse-quotient fibre estimate is the P65 interface.
- The four-marker CRT--Linnik construction and common-order table are the
  P209 interface.
- The Hilbert--90 sampler and Miller chain are restated from the
  verifier-backed P208 / F242 V2 interface.
- V3 grants all semantically valid common-order tokens.  This strengthening
  avoids assuming a hidden implementation of order certification.
- No numerical computation, remote run, web search, human audit, or
  publication-level literature review was used.
