# F238 provenance

The closest prior result is F235/P204.  It proves the same direct-factor
idea only in the balanced zero-defect branch, where the two local two-adic
valuations are equal and `N-1` contains both full local two-Sylow orders.

F238 removes both hypotheses.  It uses the universal semiprime identity

\[
 \gcd(N-1,p-1)=\gcd(p-1,q-1)
\]

and derives the exact Miller law for unequal conditional two-primary
kernels.  It also strengthens full saturation to the quantitative condition
that either remaining coprime residual is only quasipolynomial.

The multi-prime-support lower bound is a direct extension of the same
kernel argument.  It is separated from the exact squarefree-semiprime law
because repeated prime powers can create additional partial-power gcd
successes.

No computation was used.
