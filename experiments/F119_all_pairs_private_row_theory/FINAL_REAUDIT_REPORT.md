# F119 final re-audit of corrected theorem

## Verdict

**PASS** for the corrected theorem at these exact hashes:

```text
RESULT.md        552a54c8382341709305e53bd535efd9ef0ec0bc7957fa32be8ddc5c061f4575
FAILED_ROUTES.md be2afb89c6299e8de5ca0b818796d12994bb51f8a0ba912c020a908066eca563
MANIFEST.md      0eae5b2c53fa87b54b4f9d3f4c99faee205e3dc5f6b18278a5e799089835308d
```

This is a new verdict on the corrected bytes. It is not a retroactive clean
verdict on the original `RESULT.md`. The original hash
`990952a8175733959ef8d7f67429a330c27378ba897426a81c11f536dbd8a500`
retains the historical `PASS_WITH_CORRECTIONS` verdict.

No further mathematical correction is required for the theorem now stated.

## Correction closure

The original ambiguity was whether the endpoint sign-screen conclusion could
be read as applying to a different earlier residue that represented the same
exact value. The corrected text closes every route to that interpretation.

1. The theorem now says: “for every named exponent-two residue `c`, both sign
   screens of `c` and its canonical inverse have gcd one.”
2. The proof derives only `c^2=+1` or `c^2=-1` modulo a factor and rules those
   out by `0<c^2-1<c^2+1<Q_t<s`.
3. It states that an earlier occurrence of the same canonical residue has the
   same endpoints and is covered.
4. It states that a different earlier residue with the same exact value is not
   covered by the sign-screen argument.
5. The full-source boundary repeats that no null-screen claim is made for such
   a different representative.
6. `FAILED_ROUTES.md` records both the different-representative gap and the
   uncontrolled earlier unselected positions.

These statements are mutually consistent and have the correct mathematical
scope.

## Why value privacy still survives deduplication

For each named word `c`, the construction proves

```text
P_c = 1+(c-1)N_t,
P_c = q_c mod q_c^2.
```

It also proves that `q_c` divides no other selected exact value. This property
belongs to the positive integer `P_c`, not to its raw endpoint provenance.
If a different residue `d` first represents the same exact value, then
`P_N(d)=P_c` as integers. Its `q_c`-valuation is therefore still one, and the
other selected values are still nonzero modulo `q_c`.

The protected identity row and selected-column independence survive global
exact-value deduplication. Only the endpoint sign-screen statement remains
representation-specific. The corrected theorem makes exactly this split.

The sentence that exact-value deduplication “keeps all `M_t` values” is sound
when read at value level: the final distinct-value set contains each `P_c`
once. It does not claim that the named raw occurrence is the retained
representative, and the following corrected paragraphs say so explicitly.

## Focused mathematical recheck

The correction did not alter the construction. The hostile audit rechecked
the following dependencies against the corrected text:

- The three exact carry gcds are equal by subtraction and
  `gcd(1+kN,N)=1`.
- A prime row lies in the single carry class `k=-N^{-1} mod r`.
- The diameter row-degree bound and the squarefree private-row implication are
  exact.
- The base primes, protecting primes, and CRT moduli have the claimed counts
  and are pairwise coprime.
- The CRT class is reduced.
- Bertrand supplies `p_t`; Dirichlet supplies `ell_t`; Linnik bounds its size;
  and `N_t=R_t mod Q_t` follows.
- `log Q_t`, `log N_t`, and `n_t` are all controlled as stated, giving
  `M_t=Theta(n_t/log n_t)`.
- Both semiprime factors exceed `n_t^2`, so the family is trial-hard.
- The bases lie in `2..n_t`, exponent two lies in `0..n_t^2`, and each named
  word is below `N_t`.
- `N_t=1 mod c` gives the displayed least positive inverse and carry `c-1`.
- `P_c=q_c mod q_c^2` gives valuation one, while the carry-difference residue
  excludes `q_c` from every other selected value.
- The `q_c` rows form an identity submatrix, so the selected columns are
  linearly independent.
- The named-residue endpoint sign proof is valid for both prime factors and
  only for the scope now claimed.

No step depends on the removed broader interpretation.

## Strongest theorem after correction

For every sufficiently large `t`, there is an odd, non-perfect-power
semiprime `N_t=p_t ell_t`, with distinct factors greater than `n_t^2`, and

```text
M_t=t(t-1)=Theta(n_t/log n_t)
```

distinct nonunit exact values generated by genuine exponent-two unordered
seed-pair words such that:

1. every selected exact value has its own valuation-one prime row;
2. that row occurs in no other selected exact value;
3. the selected square-class columns are linearly independent;
4. both sign gcds are one for every named residue `c` and its canonical
   inverse, including an earlier occurrence of that same residue;
5. exact-value deduplication preserves every protected value, but no sign
   claim is made for a different earlier residue representing it.

The inputs can be chosen pairwise distinct along an infinite subsequence.

## Unchanged boundaries

The theorem is about a selected submatrix inside the complete mathematical
no-stop source. It does not prove:

- stable privacy after seed, frozen, and all unselected pair columns;
- absence of a proper direct sign gcd at an earlier unselected source
  position;
- rank deficiency or a nonzero exact square dependency;
- a non-global root;
- a complete-source obstruction or forced-progress theorem;
- an arbitrary-input factoring algorithm, density claim, or probability law.

The exact missing conditions remain `(FSP)` for full-source stable privacy,
`(CLOSE)` for a nonzero square dependency, and `(ROOT)` for a non-global root.

## Audit method

This was a proof-only re-audit. No mathematical computation, external search,
or empirical evidence was used. SHA-256 was used only for artifact integrity.
No durable ledger was edited.
