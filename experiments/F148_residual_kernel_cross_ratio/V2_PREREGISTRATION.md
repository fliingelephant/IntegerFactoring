# F148 V2 preregistration — deduplicated residual-kernel certificate

Use the V1 frozen corpus and loop order. Add these requirements:

1. the two centers and canonical residues are distinct;
2. all four canonical/lifted exact values are distinct;
3. the exact selected product uses those four globally deduplicated values;
4. because each actual relation value is `1 mod N`, its normalized root is
   the positive integer square root modulo `N`, with no extra division.

Stop at the first certificate meeting the corrected conditions. Preserve V1
and its rejected output.
