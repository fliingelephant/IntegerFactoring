# F233 provenance

The user suggested combining beta-two carry information, Las Vegas
sampling, integer witnesses, and lcm accumulation of common-order
certificates.  P197 formalized the lcm drift target.  P200 isolated the
zero-defect quotient branch.  P202 proved the exact return law for a long
factored word and left two large rough exclusive residuals as the hard
state.

F232 proposed aggregating factorizations of shifted quotient children into
the P202 word.  Two preregistered numerical packets failed hostile pre-run
review and were not executed.  Their failures concerned evidence workflow
and implementation, not the symbolic gcd identities.

F233 strips the proposal to its exact mathematical core.  Reducing
`BH=N-1` modulo an exclusive residual prime turns every shifted-child hit
into a ratio condition on the actual integer gap `q-p`.  A rectangular
menu is then a finite projective cover.  Elementary pigeonhole and root
counting show that its universal coverage scale is only `Theta(UC)`, which
is itself numerical QP and can be absorbed by the existing smooth word.

No computation, web search, literature claim, or durable-ledger edit is
part of this candidate.

