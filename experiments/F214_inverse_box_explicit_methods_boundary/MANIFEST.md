# F214 manifest

## Family

F214: full-\(K\) inverse-box collapse and explicit-methods boundary.

## Namespace check

Before the directory was created, a repository-wide search found no F214 or
`inverse_box_explicit` entry in the durable ledgers, notes, or experiment
tree.

## Status

Frozen, self-audited, proof-only candidate. It has not passed a fresh hostile
audit or a strict statement-only reconstruction.

## Frozen candidate files

- `STATEMENT.md`
  - SHA-256:
    `4254fce5cbd941e92328af90ae13adc8bd896e0559cdb5bff7bf92c12705d26f`
- `PROOF.md`
  - SHA-256:
    `47709b22bd4d53a7e4a2be48c092f53be6a369add17007a547672db603b80eee`
- `SELF_AUDIT.md`
  - SHA-256:
    `7964f48d43e34f853dd3b965a393ed42e395426277550de071004be154157f4f`
- `PROVENANCE.md`
  - SHA-256:
    `fe22a4be53d8603736d462488fc4dd8480dc9305aa098d74dd24c72ff84085e2`

The manifest does not hash itself. Its observed SHA-256 must be reported
separately with the frozen packet.

## Evidence class

No mathematical computation, finite search, random sampling, local
experiment, or remote experiment was run. Hashing was used only to freeze
the text.

No durable registry, proved ledger, failed ledger, progress ledger,
statement ledger, process ledger, or inspiration file was changed.

## Claims frozen for audit

1. For a balanced odd semiprime, every integer point in the full balanced
   box satisfying \(XY\equiv N\pmod{(N-1)/2}\) is exactly \((p,q)\).
2. If \(\operatorname{lcm}(2,m)\) exceeds both balanced interval
   diameters, every nonempty odd representative progression is a singleton,
   and the F209 endpoint predicate is exactly \(XY=N\).
3. A hypothetical numerical-QP full-\(K\) selector has only one near-size
   recursive child. The P183 recurrence absorbs all fixed-ratio side calls.
4. A numerical-QP list containing a true factor residue at modulus
   \(m\geq N^{1/4}/2^{(\log n)^{O(1)}}\) terminates through the
   Gao--Feng--Hu--Pan theorem cited by P175.
5. Literal interval and unit scans have \(\Theta(\sqrt N)\) candidates.
   Explicit paired-CRT half lists have size at least \((K/2)^{1/4}\).
6. Every fiber of \(u\mapsto u+u^{-1}\) modulo \(K\) has at most
   \(4\,2^{\omega(K_{\rm odd})}\sqrt K\) elements. Hence the global sum
   image has the explicit lower bound in (12), and an explicit CRT-MCSS
   half-list has size \(K^{1/4-o(1)}\).
7. The determinant-one completion \(XY-2K=1\) is exactly divisor selection,
   not an independent continued-fraction approximation.
8. The direct bivariate polynomial has \(AC=\Theta(W)\), outside the proved
   \(AC<W^{2/3}\) sufficient range. This is theorem-range
   nonapplicability only.
9. On odd \(K\), each balanced odd interval has
   \(K-\gcd(K,L)+1=K-o(K)\) nonzero Fourier modes. This obstructs only
   termwise materialization.
10. None of the explicit-method statements is a lower bound against a
    compressed exact counter, implicit CRT or MCSS, non-termwise harmonic
    evaluator, nonstandard lattice, or new integer-specific selector.

## Highest-risk points

1. Recheck both strict full-box product inequalities against
   \(K=(N-1)/2\).
2. Reconstruct the square-root counts modulo odd prime powers and powers of
   two, including every lift multiplicity.
3. Verify the uniform Rosser--Schoenfeld totient consequence and the
   elementary \(2^{\omega(K)}=K^{o(1)}\) derivation.
4. Confirm that CRT makes the global sum image the exact Cartesian product
   of local sum images and that the MCSS conclusion is restricted to
   explicit half lists.
5. Check the scaled height and exact corrected bivariate Coppersmith theorem
   interface.
6. Verify that the Fourier support count uses odd \(K\), \(L<K\), and no
   wraparound.
7. Reject any interpretation of recurrence accounting as construction of
   the missing inverse-box selector or as an all-input factoring algorithm.
8. Reject any interpretation of a named-model cardinality as a universal
   computational lower bound.

## Required fresh reviews

1. Recompute all four frozen content hashes before reading the packet.
2. Run a hostile audit against the frozen statement and proof, with special
   focus on the fiber bound and theorem scopes.
3. If it passes, give a fresh agent only `STATEMENT.md` and the key ideas
   “full-\(K\) product window, odd-step singleton, prime-power sum fibers,
   explicit representation cardinalities,” and require a strict end-to-end
   reconstruction.
4. Promote only after both reviews pass and all frozen hashes are rechecked.
