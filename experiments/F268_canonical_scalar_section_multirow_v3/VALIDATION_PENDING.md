# F268-D03 validation status

F268-D03 has not been compiled or executed. No C++ self-test, corpus
generation, preflight, discovery, heldout evaluation, evidence replay, label
audit, local numerical search, or remote command has run.

D03 is the narrow pre-run successor to immutable D02. D02 failed its fresh
hostile audit because its prose assigned 200,000 candidates to every prime
request while its marker source used 128 candidates, and because its runner
recognized only selected F265 search and runner command lines. D02 produced no
dynamic output.

D03 freezes the source's original resource-safe marker law: two requested rows
per registered cell, 4,096 pair attempts per requested row, and 128 candidates
per constrained-prime request. It does not expand the nested cap to 200,000 or
change the marker candidate stream. The frozen runner now refuses every visible
process command that contains `F265` or `f265` before target creation and before
each target phase.

Completed checks are static only:

- the exact algebra and closest-route distinctions remain unchanged;
- the complete low-support chronology remains unchanged across algebra,
  preregistration, source, self-test, and audit request;
- the named marker caps in source and preregistration agree;
- discovery ranking still has an arithmetic-only interface;
- `FAMILY_SYNTAX.tsv` has one compiled SHA-256 in C++ and the runner;
- `bash -n remote_run.sh` passed;
- the D01 and D02 immutable files were not changed;
- `git diff --check -- experiments/F268_canonical_scalar_section_multirow_v3`
  passed; and
- no durable research ledger was edited by this packet author.

Static review does not establish C++ syntax, runtime correctness, resource
feasibility, marker availability, or a mathematical search result. A fresh
no-context hostile audit is required before the target sequence. A hostile
`PASS` still does not replace compile, self-test, complete-corpus, largest-bank
preflight, replay, and label-audit gates.
