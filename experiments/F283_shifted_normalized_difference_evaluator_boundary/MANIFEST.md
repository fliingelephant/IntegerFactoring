# F283 manifest — shifted normalized-difference evaluator boundary

## Status

F283 is a frozen proof-only candidate. It is not promoted. A fresh
no-context hostile proof audit and a separate fresh statement-only
reconstruction are required.

No source code, evaluator, runner, local or remote research run, benchmark,
dataset, empirical result, proof-ledger edit, failure-ledger edit, progress
edit, registry edit, staging action, or commit belongs to this packet.

## Frozen mathematical artifacts

The exact SHA-256 values are recorded in FROZEN.sha256. That file
authenticates STATEMENT.md, PROOF.md, SELF_AUDIT.md, PROVENANCE.md, and this
manifest. Later hostile or reconstruction reports are post-freeze evidence
and must not be added to FROZEN.sha256.

## Exact boundary

For the P230 target

\[
 F_B(a)=\frac{\Delta^BX^{2B}|_{X=a}}{B!}
       =h_B(a,a+1,\ldots,a+B),
\]

the packet proves:

1. the exact generalized/r-Stirling, generating-function, and bidiagonal
   matrix representations;
2. \(B+1\) distinct generic poles and the resulting minimal order of the
   full canonical linear coefficient state, including fixed-radix
   decimation;
3. the full-width translation, reflection, first-shift, and direct
   block-convolution laws;
4. the factor-bearing first crossing in monotone divided-power composition;
5. the raw endpoint zero modulo \(N\);
6. the interval-prime product dividing the content, with content gcd exactly
   \(q\) on the balanced promise;
7. the central-binomial leading coefficient;
8. loss of the universal \(q\)-zero after content quotienting; and
9. failure of the immediate raw-lift, divide-by-\(N\), then gcd proposal,
   together with the characteristic-size faithful modulus \(NB!\).

## Search disposition

No finite synthesis run is justified in these named grammars. Each one has
an exact characteristic-width state, a two-child linear leaf count, a
factor-bearing normalization, the raw zero, an explicit interval-content
factor gate, or a quotient with no promised factor.

A future run needs a specified outside-scope state or operation with an
exact path to \(\exp(\operatorname{polylog}\log B)\) work, unit-safe public
operations, support for an arbitrary public random shift, and a proved
factor-asymmetric endpoint.

## Exclusions and audit interfaces

F283 gives no evaluator, factoring algorithm, general Mahler obstruction,
arithmetic-circuit lower bound, nonlinear-state lower bound, adaptive-state
lower bound, black-box reduction to content, or all-input theorem.

The hostile reviewer must authenticate FROZEN.sha256 and every entry. It
must check every index, factorial, valuation, interval endpoint, pole claim,
decimation claim, translation coefficient, reflection, first-shift law,
Pochhammer alias, parity split, addition-chain scope, content argument,
quotient argument, and exclusion. It must reject any inference of a general
evaluator lower bound.

The statement-only reviewer receives only the authenticated STATEMENT.md.
It must reconstruct every claimed identity and boundary without reading
PROOF.md, SELF_AUDIT.md, PROVENANCE.md, or any hostile report.
