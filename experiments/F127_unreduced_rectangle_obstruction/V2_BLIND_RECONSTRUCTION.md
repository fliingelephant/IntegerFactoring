# F127 V2 second proof-blind reconstruction — PASS

A fresh reader used only the expanded V2 statement and the named key ideas.
It independently reconstructed all claims.

The reconstruction proved:

1. \(N\ge h^2\), the canonical-carry formula, and compatibility
   \(K_m\equiv K_d\pmod d\) for \(d\mid m\);
2. the unique nested digits, both congruences, the residual formula, and
   \(|\Omega_{\square}|<2uab\max(a,b)\);
3. the endpoint identities that reduce the two sign gcds to divisors of
   \(c^2-1\) and \(c^2+1\);
4. the separate \(2R^4\) and \(R^6+1\) least-prime thresholds;
5. the exact ordered-triple tail count and its quasipolynomial numerical
   consequence;
6. both fixed carry tuples and residuals, together with infinite balanced
   prime choices in the named arithmetic progressions; and
7. the uniform constant that defeats every literal finite bank.

The reader found only one formal omission: the displayed menu fraction needs
\(Q\ge1\). The final statement now includes that assumption. No proof step
changed.

The earlier proof-blind failure remains preserved separately. It applied to
the original statement, which did not define all objects that it asked the
reader to reconstruct.
